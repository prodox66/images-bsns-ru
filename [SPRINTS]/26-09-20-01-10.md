# Спринт 2026-09-20 01:10 МСК

1. [~] Исправить ложную ошибку после успешной загрузки и сделать административные ошибки диагностируемыми.
   - Установленная причина: регулярное выражение проверки имени выдало `Unknown modifier ']'`; файл и каталог при этом сохранились.
   - Граница правки: `ResourceCollection::validatedUploadName <- ResourceEngine::storeUpload`; `BZNResourceAdmin::fetchJson <- admin actions`.
   - Исправлено: проверка имени вынесена в один метод без неоднозначного delimiter; PHP warnings преобразуются в JSON с диагностическим кодом; клиент показывает action, HTTP status и безопасный фрагмент некорректного ответа.
   - Проверено: JS syntax, admin/client contracts, PHP lint и изолированный PHP runtime-contract прошли. Осталось: локальный коммит, публикация, server fast-forward и один автоматически удаляемый HTTP upload-probe.

2. [ ] Подключить библиотеку изображений основного редактора к новому Resource Engine через resolver.
   - Загруженная пользователем картинка сохранена; общее количество выросло с 175 до 176.
   - Причина отсутствия в редакторе: текущая библиотека продолжает читать старый статический `files.js/data` и ещё не переключалась.
   - Зависимость: пользовательское подтверждение исправленного upload-ответа; старый источник сохраняется до подтверждённого переключения.
