# Спринт 26-09-07 21:04 MSK

1. [+] Подготовить `images.bsns.ru` как статическую библиотеку изображений с раздельным Git/FTP-хранением.
   - Перенесено из `26-09-07-20-20-commit.md`: код и генератор готовы; реальный FTP-контент и server generation ещё не подтверждены пользователем.

2. [+] Не блокировать библиотеку и Lightbox при временно отсутствующем `images/files.js`.
   - Сообщено пользователем: Canvas показывает «Серверная библиотека изображений недоступна»; на images должен быть Lightbox; алгоритм библиотек должен быть единым.
   - Браузерная причина: сайт `images.bsns.ru` отвечает, `images/files.js` возвращает 404, каталог `images/` возвращает 403 и не может быть перечислен браузером.
   - Сделано: общий Library Window и Lightbox загружаются до индекса; отсутствующий индекс становится пустой страницей; provider и кнопка остаются активны; Refresh повторяет загрузку и восстанавливает карточки после появления `files.js`.
   - Не затронуто: Pixabay, его proxy/provider и WebGPU route.
   - Проверено: syntax; contract 34/34; browser 3/3, страницы 30/4, Lightbox, checker, восстановление после первого искусственного 404, pageerror 0.
   - Остаётся: фактическое размещение/генерация `images/files.js` в server web-root и пользовательская проверка.

3. [+] Обеспечить генерацию статической библиотеки на старом Node.js хостинга без PHP.
   - Сообщено пользователем: `build-image-library.mjs` завершился на сервере с `SyntaxError: Unexpected token {` в ESM import.
   - Причина: установленный Node запускает `.mjs` через старый CommonJS loader и не поддерживает `node:fs/promises`.
   - Сделано: добавлен CommonJS-генератор `build-image-library-legacy.js` с тем же порядком, MIME, `files.js` и `data/NNN.js` контрактом.
   - Проверено: syntax; modern/legacy contract 34/34 с одинаковыми именами и payload mapping; повторный browser 3/3, страницы 30/4, Lightbox, pageerror 0.
   - Подтверждено пользователем: файл доставлен через SCP; серверный запуск создал индекс для 132 файлов и 174301620 байт исходных изображений, завершившись `[OK] Индекс создан`.
   - Подтверждено браузером: `https://images.bsns.ru/` показывает «Готово: 132 изображений», кнопка открытия активна.
