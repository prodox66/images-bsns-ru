# Спринт 2026-09-20 00:51 МСК

1. [!] Создан и развёрнут параллельный Resource Engine v2: публичные чёрные функции списка, ресурса, оригинала и эскиза; единая встраиваемая админка загрузки, тегов, перестроения, WebP, эскизов и удаления. `pictures`, `images`, `masks` и `shadows` пока одной константой выбирают общий каталог `resources`; неизвестный тип возвращает постоянный `UNAVAILABLE`. Позже resolver может выбрать отдельный маршрут без изменения интерфейса. Оригиналы при оптимизации сохраняются. Старый PHP масок и старый статический индекс не удалялись и не переключались. JS syntax и два contract-теста прошли 3/3; все PHP-файлы прошли lint на серверном PHP 8.2.33, изолированный core runtime-test с GD/WebP прошёл. Поскольку `images.bsns.ru` настроен как статический домен, PHP исполняется через тонкий шлюз `masks.bsns.ru/resource-admin/`, а общий каталог остаётся в `images.bsns.ru/images`. API увидел 175 ресурсов; прямое чтение PHP-исходников на статическом домене закрыто правами файла. Пользовательская загрузка физически прошла, но PHP warning из проверки имени испортил JSON-ответ, поэтому интерфейс показал общую ошибку.

## Контрольная точка

- Локальная страховочная ветка до изменений: `codex/local-pre-resource-engine-20260920-002810`.
- Серверная страховочная ветка до изменений: `codex/server-pre-resource-engine-20260920-002813`.
- Исходный commit обеих точек: `8aab10d76a1dea236f7e009db83ae10518e71985`.
- Массовая конвертация и генерация всех эскизов не запускались.
- Коммит движка: `2af14cc`; сервер обновлён только fast-forward, обе серверные рабочие копии после развёртывания чистые.
