# Спринт 26-09-07 21:15 MSK

1. [+] Подготовить `images.bsns.ru` как статическую библиотеку изображений с раздельным Git/FTP-хранением.
   - Перенесено из `26-09-07-21-04.md`: сервер видит 132 изображения; пользователь ещё не подтвердил карточку и Lightbox.

2. [+] Не блокировать библиотеку и Lightbox при временно отсутствующем `images/files.js`.
   - Перенесено из `26-09-07-21-04.md`: локально проверено 3/3; ожидается пользовательская проверка после публикации кода.

4. [+] Запускать индексатор после изменения одного FTP-файла без PHP.
   - Требование пользователя: изменение одного файла должно запускать индекс.
   - Решение: видимый `images/REINDEX_LIBRARY.trigger`; CRON вызывает лёгкую проверку; полная генерация выполняется только при новой метке/размере триггера.
   - Защита: atomic lock исключает параллельные сборки; stale lock восстанавливается; состояние пишется только после успешной генерации.
   - Проверено: syntax; trigger contract — missing/changed/unchanged/changed; modern/legacy data contract 34/34; browser 3/3, страницы 30/4, Lightbox, pageerror 0.
   - CRON: неизменённая проверка не пишет лог; сообщение появляется только после фактической сборки.
   - Подтверждено пользователем: `reindex-on-trigger.js` дважды успешно передан через SCP; повторная передача безопасно перезаписала тот же файл.
   - Подтверждено пользователем: CRON создан в экспертном режиме с расписанием `* * * * *` и полной командой trigger-обработчика.

Итоговое подтверждение пользователя по основному результату спринта: «все основное подтверждаю».
