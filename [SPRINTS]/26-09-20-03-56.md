# Спринт 2026-09-20 03:56 МСК

1. [~] Сортировать серверную библиотеку ресурсов по дате: новые первыми.
   - Причина: каталог Resource Engine сортировался по имени, поэтому загруженный файл было трудно найти и проверить.
   - Граница: изменён только канонический порядок каталога; фильтры, пагинация, загрузка, эскизы и старый статический fallback не менялись.
   - Граф: `ResourceEngine::sortNewestFirst <- ResourceEngine::rebuildUnlocked`; API и админка получают этот порядок через `ResourceEngine::list`.
   - Реализация: `modified DESC`; при одинаковом времени — естественный порядок имени для стабильного результата.
   - Проверено: `git diff --check`, JS syntax, Resource Client/Admin contracts; в изолированной серверной копии PHP lint и полный Resource Engine contract — `OK`.
   - Публикация: функциональный commit/push и server fast-forward выполняются после этой контрольной записи; пользовательская проверка остаётся обязательной.
