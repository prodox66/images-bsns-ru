# Спринт 24.09.2026 01:37 МСК — пакетная оптимизация общей галереи

## Легенда

- [+] — пользователь явно подтвердил работу.
- [~] — правка опубликована, пользователь ещё не подтвердил.
- [!] — пользователь сообщил о сбое.
- [ ] — не начиналось; указана причина.
- [-] — точечно отменена собственная неудачная правка.

## Очередь

90. [~] [#~] Серверная часть админской пакетной оптимизации общей галереи.
   - Существующий `ResourceEngine::optimizeBatch()` теперь создаёт WebP 80% максимум 2048 px по длинной стороне без увеличения малых изображений.
   - Оригиналы общей галереи сохраняются. Параметры WebP входят в версию каталога производных, поэтому прежние полноразмерные WebP не считаются готовыми и не перезаписываются до успешного создания новой версии.
   - Проверено на PHP 8.2/GD в изолированной серверной копии: синтаксис трёх PHP-файлов; полный контракт, включая 24×12 -> 10×5, 4×2 -> 4×2, сохранённый PNG-original, два ограниченных пакета и смену профиля.
   - Следующая связанная часть в `design-bzn-ru`: администраторская кнопка, контекстная команда, прогресс и явный `kind=original` для кропера. Автоматическая оптимизация новых копий из Pixabay, генераторов, ComfyUI, Яндекс Диска и загрузки — следующий отдельный этап.
