# Спринт 26-09-07 20:20 MSK

1. [+] Подготовить `images.bsns.ru` как статическую библиотеку изображений с раздельным Git/FTP-хранением.
   - Код: автономная страница, central runtime config, object provider, shared Library Window/Lightbox.
   - Контент: `images/*` исключён из Git; реальные файлы будут скопированы из прежнего server `any_bg` либо загружены по FTP.
   - Генератор: сканирует фактически найденные PNG/JPG/WebP/AVIF/GIF/SVG и создаёт files.js/data scripts.
   - Настройки: default tab `Images`, page size30, caller/content type, пути, globals, MIME, labels/status собраны в одном `SITE_SETTINGS`.
   - Уточнение: числовая геометрия больше не дублируется в content site; все галереи получают page size из общего `library_window.json`. Для Images в общей UI-теме добавлена светло-серая checker-подложка миниатюр и Lightbox; masks остаются белыми.
   - Проверено: syntax5; contract34/34 exact index/data; Chrome3/3, страницы30/4, Images tab, обычный Lightbox, pageerror0.
   - Повторно проверено после общей геометрии/checker: contract34/34; Chrome3/3, страницы30/4, checker карточек и Lightbox, pageerror0.
   - Остаётся: server checkout/pull, перенос реальных изображений и интеграция Canvas.
