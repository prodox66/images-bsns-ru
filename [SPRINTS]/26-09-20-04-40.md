# Спринт 20.09.2026 04:40 МСК

1. [~] Закрытый HTTP Resource API для внешних сайтов, VPS и статических storage/CDN.
   - PHP endpoint живёт на `images.bsns.ru`; `files.bsns.ru` остаётся сменным статическим хранилищем.
   - Межсерверный запрос передаёт логический тип, scope и непрозрачный owner; physical path и service key не выходят в браузер.
   - Первая коллекция: `templates`, scope `demo/user`, исходники в runtime-каталогах `files.bsns.ru`.
   - Использовать существующий Resource Engine для list/original/thumbnail, а не создавать второй движок.
   - Добавлены `service-api.php`, service-key boundary, dynamic demo/user context и reuse существующих list/original/thumbnail.
   - `ResourceCollection` получил необязательный filename policy; обычная коллекция `resources` без него сохраняет прежнее поведение.
   - До изменения local/GitHub/server чисты и совпадают; созданы страховочные ветки.
   - PHP lint, прежний Resource Engine contract и новый service contract прошли; service contract выполнен 3 цикла.
   - Нужны commit/push, deploy и живой HTTP-тест.

2. [ ] Подключить same-origin gateway редактора и `Демо / Мои шаблоны`.
   - Зависит от живого server-to-server endpoint пункта 1.
   - Выполняется отдельным commit в `design-bzn-ru`.
