# Спринт 20.09.2026 04:40 МСК

1. [-] Закрытый HTTP Resource API непосредственно на `images.bsns.ru`.
   - PHP endpoint живёт на `images.bsns.ru`; `files.bsns.ru` остаётся сменным статическим хранилищем.
   - Межсерверный запрос передаёт логический тип, scope и непрозрачный owner; physical path и service key не выходят в браузер.
   - Первая коллекция: `templates`, scope `demo/user`, исходники в runtime-каталогах `files.bsns.ru`.
   - Использовать существующий Resource Engine для list/original/thumbnail, а не создавать второй движок.
   - Изолированные PHP lint, прежний Resource Engine contract и новый service contract прошли; service contract выполнен 3 цикла.
   - Живой HTTP-тест доказал, что `images.bsns.ru` статический: новый `.php` был отдан как исходный файл, а прежние PHP-пути закрываются nginx с 403.
   - Собственный service entrypoint, классы, config, tests и точечные изменения общих классов полностью удалены. Старый Resource Engine и runtime-картинки сохранены.
   - Вычислительный endpoint переносится в PHP-репозиторий `library-api-bsns-ru`; `images/files` остаются сменными static storage/CDN.

2. [ ] Подключить same-origin gateway редактора и `Демо / Мои шаблоны`.
   - Зависит от живого server-to-server endpoint пункта 1.
   - Выполняется отдельным commit в `design-bzn-ru`.
